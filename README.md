# Lata Yog Routine Guide

A modern, responsive React web application designed to guide users through structured yoga routines. Built with Vite, Tailwind CSS, and Framer Motion.

## Setup Instructions
1. Install dependencies: `npm install`
2. Start the development server: `npm run dev`
3. Build for production: `npm run build`
